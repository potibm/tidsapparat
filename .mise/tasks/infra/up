#!/usr/bin/env bash
#MISE description="Start infrastructure services"

docker compose up -d --force-recreate --remove-orphans

echo ""
echo "🚀 --- INFRASTRUCTURE DASHBOARD ---"
echo "📊 OpenObserve:   https://openobserve.tidsapparat.test (admin@example.com / password123)"
echo "📊 RedisInsight:  https://redis.tidsapparat.test" 
echo "📊 MinIO:         https://minio.tidsapparat.test (local-admin / local-super-secret)"
echo "🪪 Dex OIDC:      https://dex.tidsapparat.test"
echo "----------------------------------"
